# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.netlify/

# Claude Code — local runtime state, worktrees, secrets (never commit)
# Pattern uses /* (not /) so exceptions below can re-include specific files
.claude/*
# Exception: project-wide hook config must sync across devices
!.claude/settings.json

# Raw data sources used to regenerate public/data/specialites/ — kept local only
Extract GIE/

# PDFs sources du rebranding des rapports (174 Mo) — travail local, trop lourd pour git
reports-rebrand/
